# Template only — never commit a real secret. On Devtron use the GUI secret
# ({project}-{app} naming) with envFrom into the workload instead.
apiVersion: v1
kind: Secret
metadata:
  name: daily-horoscope-secrets
type: Opaque
stringData:
  DATABASE_URL: postgres://user:pass@host:5432/horoscope
  PGSSL: require
  GEMINI_API_KEY: replace-me
